import { BrowserRouter as Router, Route, Switch} from 'react-router-dom';
import login from '/login/login.jsx';
import register from 'login/register/';

function Rotas() {
    return (
        <Router>
            <Switch>
                <Route path='/login/login.jsx' Component={login}></Route>
                <Route path='login/register.jsx' Component={register}></Route>
            </Switch>
        </Router>
    )
}